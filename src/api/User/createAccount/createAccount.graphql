type Mutation {
  createAccount(
    userId: String!
    password: String!
    username: String!
    nickname: String!
    email: String!
    phoneNum: String!
    address: String
    isAgree: Boolean!
  ): Boolean!
}

type Query {
  something: String
}
